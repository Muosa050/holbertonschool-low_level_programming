#include "main.h"
#include <string.h>
/**
*_strlen - function that return the legnth of str
*
*@s: string to be calculated
*
*Return: a as a length of string s
*/
int _strlen(char *s)
{
int a = strlen(s);
return (a);
}
